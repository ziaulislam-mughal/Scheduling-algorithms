# Scheduling-algorithms
Implement the following scheduling algorithms  FCFS  , SJF (both preemptive and non-preemptive)  , priority scheduling (both preemptive and non-preemptive) , Round Robin in any programming language. Finally calculate wait  time for the number of jobs selected in each policy. 
